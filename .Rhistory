sample_rows
# Set seed for reproducibility
set.seed(123)
# Create a dummy data frame with 10 rows
df <- data.frame(
ID = 1:10,
Name = paste("Name", 1:10),
Age = sample(20:40, 10, replace = TRUE)
)
# View the full data
print("Full Data Frame:")
print(df)
# Randomly sample 5 row numbers
sampled_rows <- sample(nrow(df), size = 5)
# Use those row numbers to subset the data frame
df_sampled <- df[sampled_rows, ]
# View the sampled rows
print("Sampled Rows:")
print(df_sampled)
?sample_n
ts_viz_percentdays <- function(area = c("monterey_bay", "channel_islands", "fort_bragg", "san_francisco"),
yr_range = 1990:2100,
def = "def8",
input_file = abalone::percentdays,
cons_thresh = 95,
lib_thresh = 50,
extent_list = abalone::extent_list,
plot_title = paste0(def, " | ", area, " | % of year refugia conditions met")) {
#utils::globalVariables(c("year", "model", "val"))
# Validate area
if (!area %in% names(extent_list)) {
stop("Area not found in extent_list.")
}
# Get bounding box
ex_vals <- extent_list[[area]]
ex <- terra::ext(ex_vals["xmin"], ex_vals["xmax"], ex_vals["ymin"], ex_vals["ymax"])
# Create base raster
base_rast <- cali_rast()
n_layers <- length(yr_range)
# Preallocate raster stacks for each model
gfdl_stack <- terra::rast(ncol = ncol(base_rast),
nrow = nrow(base_rast),
nlyr = n_layers,
extent = terra::ext(base_rast),
crs = terra::crs(base_rast))  %>% terra::crop(ex)
ipsl_stack <- terra::rast(gfdl_stack) %>% terra::crop(ex)
hadtv_stack <- terra::rast(gfdl_stack) %>% terra::crop(ex)
for (i in seq_along(yr_range)) {
yr <- yr_range[i]
all_cells <- terra::cells(base_rast)
cell_coords <- terra::xyFromCell(base_rast, all_cells)
cell_df <- data.frame(cellID = all_cells, x = cell_coords[,1], y = cell_coords[,2])
cell_df_cropped <- dplyr::filter(cell_df,
x >= ex[1] & x <= ex[2] & y >= ex[3] & y <= ex[4])
# GFDL
gfdl_df <- input_file %>% dplyr::filter(model == "gfdltv", year == yr)
gfdl_df_cropped <- dplyr::filter(gfdl_df, cellID %in% cell_df_cropped$cellID)
gfdl_rast <- base_rast %>% terra::crop(ex)
gfdl_rast[terra::cells(gfdl_rast)] <- gfdl_df_cropped$percent
gfdl_stack[[i]] <- gfdl_rast
names(gfdl_stack)[i] <- as.character(yr)
# IPSL
ipsl_df <- input_file %>% dplyr::filter(model == "ipsltv", year == yr)
ipsl_df_cropped <- dplyr::filter(ipsl_df, cellID %in% cell_df_cropped$cellID)
ipsl_rast <- base_rast %>% terra::crop(ex)
ipsl_rast[terra::cells(ipsl_rast)] <- ipsl_df_cropped$percent
ipsl_stack[[i]] <- ipsl_rast
names(ipsl_stack)[i] <- as.character(yr)
# IPSL
had_df <- input_file %>% dplyr::filter(model == "hadtv", year == yr)
had_df_cropped <- dplyr::filter(had_df, cellID %in% cell_df_cropped$cellID)
had_rast <- base_rast %>% terra::crop(ex)
had_rast[terra::cells(had_rast)] <- had_df_cropped$percent
hadtv_stack[[i]] <- had_rast
names(hadtv_stack)[i] <- as.character(yr)
}
emplist <- vector("list", length = length(yr_range))
for (i in 1:length(yr_range)) {
rasty <- gfdl_stack[[i]]
total <- terra::values(rasty, na.rm=T) %>% sum # Sum all the cells in this area
total <- total/terra::values(rasty, na.rm = T) %>% length
df_gfdl <- data.frame(model = "gfdltv", val = total, year = yr_range[i])
rasty <- ipsl_stack[[i]]
total <- terra::values(rasty, na.rm=T) %>% sum
total <- total/terra::values(rasty, na.rm = T) %>% length
df_ipsl <- data.frame(model = "ipsltv", val = total, year = yr_range[i])
rasty <- hadtv_stack[[i]]
total <- terra::values(rasty, na.rm=T) %>% sum
total <- total/terra::values(rasty, na.rm = T) %>% length
df_had <- data.frame(model = "hadtv", val = total, year = yr_range[i])
df <- rbind(df_gfdl, df_ipsl, df_had)
ensdf <- data.frame(model = "ens",
year = yr_range[i],
val = (df_gfdl$val + df_had$val +df_ipsl$val)/3)
df <- rbind(df, ensdf)
emplist[[i]] <- df
}
all1 <- do.call(rbind, emplist)
# Plot
ggplot2::ggplot(data = all1, ggplot2::aes(x = year)) +
ggplot2::geom_line(data = dplyr::filter(all1, model == "gfdltv"),
ggplot2::aes(y = val, color = "gfdltv"), size = 0.4) +
ggplot2::geom_line(data = dplyr::filter(all1, model == "hadtv"),
ggplot2::aes(y = val, color = "hadtv"), size = 0.4) +
ggplot2::geom_line(data = dplyr::filter(all1, model == "ipsltv"),
ggplot2::aes(y = val, color = "ipsltv"), size = 0.4) +
ggplot2::geom_line(data = dplyr::filter(all1, model == "ens"),
ggplot2::aes(y = val, color = "ens"), size = 1.5) +
ggplot2::scale_color_manual(values = c(
"ens" = "black",
"gfdltv" = "#8E4A49",
"ipsltv" = "#BDC4A7",
"hadtv" = "#3F7CAC"
), name = "Model") +
ggplot2::labs(
title = plot_title,
x = "Year", y = "% Days"
) +
ggplot2::theme_minimal() +
ggplot2::ylim(0, 100) +
ggplot2::theme(
panel.grid.minor = ggplot2::element_blank(),
plot.margin = ggplot2::unit(c(1, 0.1, 0.1, 0.1), "cm"),
axis.title = ggplot2::element_text(size = 16, face = "bold"),
axis.text = ggplot2::element_text(size = 14),
legend.title = ggplot2::element_text(size = 14),
legend.text = ggplot2::element_text(size = 12),
title = ggplot2::element_text(size = 14)
) +
ggplot2::geom_hline(ggplot2::aes(yintercept = lib_thresh), lty = "dashed") +
ggplot2::geom_hline(ggplot2::aes(yintercept = cons_thresh), lty = "dashed")
}
ts_viz_percentdays(area = "monterey_bay", yr_range = 1990:2100, def = "def8",
input_file = abalone::percentdays, cons_thresh = 95, lib_thresh = 50)
library(abalone)
ts_viz_percentdays(area = "monterey_bay", yr_range = 1990:2100, def = "def8",
input_file = abalone::percentdays, cons_thresh = 95, lib_thresh = 50)
library(magrittr)
ts_viz_percentdays(area = "monterey_bay", yr_range = 1990:2100, def = "def8",
input_file = abalone::percentdays, cons_thresh = 95, lib_thresh = 50)
devtools::build_site()
devtools::build_site()
quarto::quarto_path()
devtools::build_site()
build_site()
devtools::build_site()
devtools::build_site()
devtools::build_site()
devtools::build_site()
devtools::build_site()
devtools::build_site()
devtools::build_site()
devtools::build_site()
devtools::build_site()
citation("abalone")
devtools::build_site()
library(ncdf4)
# Open a NetCDF file lazily and remotely
ncopendap <- nc_open("http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/cefi_portal/northeast_pacific/full_domain/hindcast/monthly/regrid/r20250912/tos.nep.full.hcast.monthly.regrid.r20250912.199301-202506.nc
")
nc_open(ncopendap)
# Open a NetCDF file lazily and remotely
ncopendap <- nc_open("http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/cefi_portal/northeast_pacific/full_domain/hindcast/monthly/regrid/r20250912/tos.nep.full.hcast.monthly.regrid.r20250912.199301-202506.nc")
nc_open(ncopendap)
# Specify the OPeNDAP server URL (using regular grid output)
url <- "http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/cefi_portal/northeast_pacific/full_domain/hindcast/monthly/regrid/r20250912/tos.nep.full.hcast.monthly.regrid.r20250912.199301-202506.nc"
# Open a NetCDF file lazily and remotely
ncopendap <- nc_open(url)
# Read the data into memory
timeslice = 1
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "sos", start = c(1, 1, timeslice), count = c(-1, -1, 1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "tos", start = c(1, 1, timeslice), count = c(-1, -1, 1))
ncopendap$var
ncopendap$filename
ncopendap$var$tos
ncopendap$var
unique(ncopendap$var)
ncopendap$var
# Read the data into memory
timeslice = 1
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
sos
# Get the units
tunits <- ncatt_get(ncopendap, "time", "units")
datesince <- tunits$value
datesince <- substr(datesince, nchar(datesince)-9, nchar(datesince))
datesince
# convert the number to datetime (input should be in second while the time is in unit of days)
datetime_var <- as.POSIXct(time*86400, origin=datesince, tz="UTC")
datetime_var
filled.contour(lon, lat, sos, main = paste("Sea surface salinity at ", datetime_var), xlab = "Longitude", ylab = "Latitude", levels = pretty(c(20,40), 20))
filled.contour(lon, lat, sos,
main = paste("Sea surface salinity at ", datetime_var),
xlab = "Longitude",
ylab = "Latitude", levels = pretty(c(20,40), 20))
maps::map("world", add= T)
df <- expand.grid(X = lon, Y = lat)
data <- as.vector(t(sos))
df$Data <- data
names(df) <- c("lon", "lat", "sos")
rr <- rast("/Users/admin/Documents/GitHub/packages/abalone/tos.nep.full.hcast.monthly.regrid.r20250912.199301-202506.nc")
library(terra)
rr <- rast("/Users/admin/Documents/GitHub/packages/abalone/tos.nep.full.hcast.monthly.regrid.r20250912.199301-202506.nc")
rr
rr[[1]]
plot(rr[[1]])
filled.contour(lon, lat, sos,
main = paste("Sea surface salinity at ", datetime_var),
xlab = "Longitude",
ylab = "Latitude", levels = pretty(c(20,40), 20))
maps::map("world", add= T)
maps::map("world", add= T, wrap = c(0, 360))
# Read the data into memory
timeslice = 5
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "tos", start = c(1, 1, timeslice), count = c(-1, -1, 1))
# Get the units
tunits <- ncatt_get(ncopendap, "time", "units")
datesince <- tunits$value
datesince <- substr(datesince, nchar(datesince)-9, nchar(datesince))
datesince
# convert the number to datetime (input should be in second while the time is in unit of days)
datetime_var <- as.POSIXct(time*86400, origin=datesince, tz="UTC")
datetime_var
filled.contour(lon, lat, sos,
main = paste("Sea surface salinity at ", datetime_var),
xlab = "Longitude",
ylab = "Latitude", levels = pretty(c(20,40), 20))
maps::map("world", add= T, wrap = c(0, 360))
# Read the data into memory
timeslice = 400
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
# Read the data into memory
timeslice = 300
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "tos", start = c(1, 1, timeslice), count = c(-1, -1, 1))
# Get the units
tunits <- ncatt_get(ncopendap, "time", "units")
datesince <- tunits$value
datesince <- substr(datesince, nchar(datesince)-9, nchar(datesince))
datesince
# convert the number to datetime (input should be in second while the time is in unit of days)
datetime_var <- as.POSIXct(time*86400, origin=datesince, tz="UTC")
datetime_var
filled.contour(lon, lat, sos,
main = paste("Sea surface salinity at ", datetime_var),
xlab = "Longitude",
ylab = "Latitude", levels = pretty(c(20,40), 20))
# Specify the OPeNDAP server URL (using regular grid output)
url <- "http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/cefi_portal/northeast_pacific/full_domain/hindcast/monthly/regrid/r20250912/tos.nep.full.hcast.monthly.raw.r20250912.199301-202506.nc"
# Open a NetCDF file lazily and remotely
ncopendap <- nc_open(url)
# Specify the OPeNDAP server URL (using regular grid output)
url <- "http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/cefi_portal/northeast_pacific/full_domain/hindcast/monthly/regrid/r20250912/tos.nep.full.hcast.monthly.regrid.r20250912.199301-202506.nc"
# Open a NetCDF file lazily and remotely
ncopendap <- nc_open(url)
# Read the data into memory
timeslice = 300
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "tos", start = c(1, 1, timeslice), count = c(-1, -1, 1))
# Get the units
tunits <- ncatt_get(ncopendap, "time", "units")
datesince <- tunits$value
datesince <- substr(datesince, nchar(datesince)-9, nchar(datesince))
datesince
# convert the number to datetime (input should be in second while the time is in unit of days)
datetime_var <- as.POSIXct(time*86400, origin=datesince, tz="UTC")
datetime_var
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "tos", start = c(1, 1, timeslice), count = c(-1, -1, 1))
sos <- t(sos)
# Get the units
tunits <- ncatt_get(ncopendap, "time", "units")
datesince <- tunits$value
datesince <- substr(datesince, nchar(datesince)-9, nchar(datesince))
datesince
# convert the number to datetime (input should be in second while the time is in unit of days)
datetime_var <- as.POSIXct(time*86400, origin=datesince, tz="UTC")
datetime_var
filled.contour(lon, lat, sos,
main = paste("Sea surface salinity at ", datetime_var),
xlab = "Longitude",
ylab = "Latitude", levels = pretty(c(20,40), 20))
maps::map("world", add= T, wrap = c(0, 360))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "tos", start = c(1, 1, timeslice), count = c(-1, -1, 1))
filled.contour(lon, lat, sos,
main = paste("Sea surface salinity at ", datetime_var),
xlab = "Longitude",
ylab = "Latitude", levels = pretty(c(20,40), 20))
# infer resolution from vectors
res_lon <- abs(diff(lon)[1])
res_lat <- abs(diff(lat)[1])
r <- rast(
xmin = min(lon),
xmax = max(lon),
ymin = min(lat),
ymax = max(lat),
resolution = c(res_lon, res_lat),
crs = "EPSG:4326"
)
r
plot(r)
plot(r, axes =T)
r
values(r) <- as.vector(sos)
as.vector(sos)
values(r) <- as.vector(sos)
r <- rast(
t(sos),
xmin = min(lon),
xmax = max(lon),
ymin = min(lat),
ymax = max(lat),
resolution = c(res_lon, res_lat),
crs = "EPSG:4326"
)
r <- rast(
xmin = min(lon),
xmax = max(lon),
ymin = min(lat),
ymax = max(lat),
resolution = c(res_lon, res_lat),
crs = "EPSG:4326"
)
values(r) <- as.vector(sos)
r <- rast(
ncols = length(lon),
nrows = length(lat),
xmin = min(lon),
xmax = max(lon),
ymin = min(lat),
ymax = max(lat),
crs = "EPSG:4326"
)
values(r) <- as.vector(sos)
r
r %>% plot
plot(r)
values(r) <- as.vector(t(sos))
plot(r)
r <- rast(
ncols = length(lon),
nrows = length(lat),
xmin = min(lon),
xmax = max(lon),
ymin = min(lat),
ymax = max(lat),
crs = "EPSG:4326"
)
values(r) <- as.vector(sos)
plot(r)
if (lat[1] < lat[length(lat)]) {
# lat increasing south → north, flip to north → south
sos <- sos[, ncol(sos):1]
}
values(r) <- as.vector(sos)
plot(r)
library(tidyverse)
library(ncdf4)
url <- "http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/cefi_portal/northeast_pacific/full_domain/hindcast/monthly/raw/r20250509/thetao.nep.full.hcast.monthly.raw.r20250509.199301-202412.nc"
nc <- nc_open(url)
#I think thetao only comes at monthly res
thetaourl <- "http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/cefi_portal/northeast_pacific/full_domain/hindcast/monthly/regrid/r20250912/thetao.nep.full.hcast.monthly.regrid.r20250912.199301-202506.nc"
nc_open(thetaourl)
nc <- nc_open(thetaourl)
# Read the data into memory
timeslice = 300
ncopendap <- nc_open(thetaourl)
# Read the data into memory
timeslice = 300
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "thetao", start = c(1, 1, timeslice), count = c(-1, -1, 1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "tos", start = c(1, 1, timeslice), count = c(-1, -1, 1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "thetao", start = c(1, 1, timeslice), count = c(-1, -1, 1))
ncopendap
c(1, 1, timeslice)
# Read the data into memory
timeslice = 100
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "thetao", start = c(1, 1, timeslice), count = c(-1, -1, 1))
# Read the data into memory
timeslice = 12
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "thetao", start = c(1, 1, timeslice), count = c(-1, -1, 1))
timeslice
ncopendap$var$thetao
ncopendap$var$thetao$dim
ncvar_get(
ncopendap,
"thetao",
start = c(1, 1, 1, timeslice),
count = c(-1, -1, -1, 1)
)
library(tidyverse)
library(ncdf4)
ncopendap
# Read a slice of the data into memory
#sos <- ncvar_get(ncopendap, "thetao", start = c(1, 1, timeslice), count = c(-1, -1, 1))
timeslice <- 2
ncopendap
thetao <- ncvar_get(
ncopendap,
"thetao",
start = c(1, 1, 1, timeslice),
count = c(-1, -1, -1, 1)
)
sapply(ncopendap$var$thetao$dim, function(d) d$name)
dims <- sapply(ncopendap$var$thetao$dim, function(d) d$len)
dims
dims <- sapply(ncopendap$var$thetao$dim, function(d) d$len)
dims
sapply(ncopendap$var$thetao$dim, function(d) d$name)
names(dims) <- sapply(ncopendap$var$thetao$dim, function(d) d$name)
dims
thetao <- ncvar_get(
ncopendap,
"thetao",
start = c(1, 1, 1, timeslice),
count = c(dims["lon"], dims["lat"], dims["z_l"], 1)
)
rast(thetaourl)
rast(thetaourl, subds="thetao")
dims <- sapply(ncopendap$var$thetao$dim, function(d) d$len)
dims
names(dims) <- sapply(ncopendap$var$thetao$dim, function(d) d$name)
dims
dims["lon"]
dims["lon"]
dims["lon"] %>% as.vector,
dims["lon"] %>% as.vector
thetao <- ncvar_get(
ncopendap,
"thetao",
start = c(1, 1, 1, timeslice),
count = c(dims["lon"] %>% as.vector, dims["lat"] %>% as.vector,
dims["z_l"] %>% as.vector, 1)
)
library(reticulate)
py_config()  # check which Python reticulate will use
xr <- import("xarray")
s3fs <- import("s3fs")
thetaourl <- "http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/cefi_portal/northeast_pacific/full_domain/hindcast/monthly/regrid/r20250912/thetao.nep.full.hcast.monthly.regrid.r20250912.199301-202506.nc"
ds <- xr$open_dataset(
thetaourl,
chunks = dict(time = 1, z_l = -1, lat = 100, lon = 100)
)
thetaourl <- "http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/cefi_portal/northeast_pacific/full_domain/hindcast/monthly/regrid/r20250912/thetao.nep.full.hcast.monthly.regrid.r20250912.199301-202506.nc"
ds <- xr$open_dataset(
thetaourl,
chunks = dict(time = 1, z_l = -1, lat = 100, lon = 100)
)
ds <- xr$open_dataset(
thetaourl,
engine = "pydap",
chunks = dict(time = 1, z_l = -1, lat = 100, lon = 100)
)
# Specify the OPeNDAP server URL (using regular grid output)
url <- "http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/northwest_atlantic/hist_run/regrid/ocean_monthly.199301-201912.sos.nc"
# Open a NetCDF file lazily and remotely
ncopendap <- nc_open(url)
# Read the data into memory
timeslice = 1
lon <- ncvar_get(ncopendap, "lon")
lat <- ncvar_get(ncopendap, "lat")
time <- ncvar_get(ncopendap, "time",start = c(timeslice), count = c(1))
# Read a slice of the data into memory
sos <- ncvar_get(ncopendap, "sos", start = c(1, 1, timeslice), count = c(-1, -1, 1))
# Get the units
tunits <- ncatt_get(ncopendap, "time", "units")
datesince <- tunits$value
datesince <- substr(datesince, nchar(datesince)-9, nchar(datesince))
datesince
# convert the number to datetime (input should be in second while the time is in unit of days)
datetime_var <- as.POSIXct(time*86400, origin=datesince, tz="UTC")
datetime_var
filled.contour(lon, lat, sos, main = paste("Sea surface salinity at ", datetime_var), xlab = "Longitude", ylab = "Latitude", levels = pretty(c(20,40), 20))
# Specify the OPeNDAP server URL (using regular grid output)
url <- "http://psl.noaa.gov/thredds/dodsC/Projects/CEFI/regional_mom6/northeast_pacific/hist_run/regrid/ocean_monthly.199301-201912.sos.nc"
# Open a NetCDF file lazily and remotely
ncopendap <- nc_open(url)
devtools::check()
devtools::install(clean = T)
